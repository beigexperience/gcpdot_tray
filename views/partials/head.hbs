<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Configure Tray Menu</title>
  <link rel="icon" type="image/png" href="/favicon.png">
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
   <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    .item { margin-bottom: 10px; cursor: move; border: 1px inset #ccc; padding: 5px; }
    button { margin-right: 10px; }
    .context-menu { display: none; position: absolute; background: white; border: 1px solid #ccc; z-index: 1000; }
    .context-menu-item { padding: 5px 10px;cursor: pointer;display: flex;align-items: center;line-height: 1;border: 1px outset seashell; }
    .context-menu-item:hover { background: #eee; }
    .context-menu-item .icon {
      margin-right: 5px;
      display: inline-block;
      width: 1em; /* Set a fixed width for the icons */
      text-align: center; /* Center the icon within the fixed width */
    }
      .dialog {
        
        text-align: center;
      padding: 1rem;
      background: white;
      border-radius: 4px;
      border: 1px solid #ccc;
    }
    .dialog::backdrop {
      background: rgba(0, 0, 0, 0.4);
    }
    .success-message {
      color: #28a745;
      display: inline-block;      
    }
    .dialog-message p{
        display: inline-block;
    }
    .dialog button[type="ok"] {
      margin: 0 auto;
      display: block;
    }
    input[name=executable],input[name=workingDir],input[name=icon] {
      display: inline-block;
      width: --webkit-fill-available;
    }
    label{
      
    }
  </style>
  <script>
    const _$id = id => document.getElementById(id);
    const _$ = selector => document.querySelector(selector);
    const _$$ = selector => document.querySelectorAll(selector);
  </script>
</head>